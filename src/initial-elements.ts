// Initial Elements go here

import { MarkerType, type Edge } from '@xyflow/react';
import { type ShapeNode } from './components/shape/types';

export const defaultNodes: ShapeNode[] = [];

export const defaultEdges: Edge[] = [];
